%h2= @survey.title

- @survey.questions.each do |question|
  .question
    %h4= question.content
    - if question.reportable?
      = hidden_field_tag "reportable_question", question.id, id: nil, class: "reportable_question"
    .chart


    - else
      = "This question type does not support report features"
