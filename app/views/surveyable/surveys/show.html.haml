%h2= @survey.title

- @survey.questions.each do |question|
  .question
    %h4= question.content
    = question.to_yaml

    - if question.reportable?
      = render "surveyable/surveys/reports/#{question.field_type}", question: question
